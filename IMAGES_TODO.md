# Images still to add

Every imported note now has its images **except** the references below — these notes point at a file that isn't in `images/notes/` yet. Save each under the exact filename and it renders automatically (no editing needed).

| Note | Missing file(s) |
|------|-----------------|
| ACT | `act-2.png` (temporal ensembling) |
| PaliGemma | `paligemma-3.png` (SigLIP), `paligemma-4.png` (normalisation), `paligemma-5.png` (KV cache) |

## Orphan

`images/notes/molmoact2-1.png` exists but **no note references it** — there is no MolmoAct page in the Notion Literature Review, so no note was created for it. Either point me at a source to write a MolmoAct note, or tell me which note this image belongs to (or delete it).
